#pragma once
#include <pch.h>
#include "LoadedAsset.hpp"
class LoadedTextureAsset : LoadedAsset
{
private:
    /* data */
public:
    LoadedTextureAsset(/* args */);
    ~LoadedTextureAsset();
};

LoadedTextureAsset::LoadedTextureAsset(/* args */)
{
}

LoadedTextureAsset::~LoadedTextureAsset()
{
}
