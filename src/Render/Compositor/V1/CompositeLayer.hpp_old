#pragma once

#include <pch.h>
#include "CompositeObject.hpp"
namespace Render
{
template <typename Payload>
class CompositeLayer : public CompositeObject<Payload,Payload>
{

    void Compose(const Payload& in, Payload& out)override {out = in;};
    public:
    virtual Payload Compose(const Payload& in) = 0;
};
}
